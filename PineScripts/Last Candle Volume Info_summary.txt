AI LIQUIDITY MAP - CANONICAL INDICATOR REFERENCE

Scope
-----

This document is the canonical reference for `Last Candle Volume Info_AI2.pine`.
It consolidates the indicator rules and the full chart-graphics reference. The
former `Last Candle Volume graphic items.txt` file now redirects here so the
visual contract has one authoritative source.

All Buy, Sell, Total, Delta, and Open Interest values are estimates derived from
chart and lower-timeframe candle data. They are not exchange bid/ask data. USDT
values are estimated volume multiplied by the chart close.

1. LIQUIDITY MAP
----------------

- Confirmed pivot highs create Buy-Side Liquidity (BSL) levels. Confirmed pivot
  lows create Sell-Side Liquidity (SSL) levels.
- Nearby same-side pivots within `tolerancePercent` are merged as EQH/EQL by
  increasing touches rather than creating duplicate levels.
- Pivot strength is prioritized as EQH/EQL, then high-volume pivots, then normal
  pivots. High volume is determined from the pivot-bar USDT volume versus the
  configured volume SMA and multiplier.
- Active BSL levels are mint/teal and active SSL levels are crimson/pink. They
  extend right. The nearest active BSL above price and nearest active SSL below
  price are solid, width-3, fully opaque lines; every deeper active level stays
  width 1 with lower opacity.
- A level is mitigated by the selected `Wick Sweep` or `Close Breach` rule.
  Mitigated levels become gray dashed history when `keepMitigated` is enabled;
  otherwise their line is deleted.
- Stored liquidity zones are capped by `maxZones`; pruning removes the oldest
  zone and its line.

2. H4 LEVELS AND STATISTICAL CORRIDORS
--------------------------------------

- Locked H4 pivot high and low are the central H4 boundaries. On lower
  timeframes their optional projections are mint/pink dashed lines.
- The orange line above H4 High is the expected historical sweep deviation
  (`h4UpperCorridor`). The orange line below H4 Low is the corresponding lower
  deviation (`h4LowerCorridor`).
- The semi-transparent orange areas between H4 High/Low and these boundaries are
  statistical sweep corridors, not automatic entries.
- The top-right H4 panel shows distance from H4 High / Low, reversal probability,
  HTF trend and session context, recent CVD divergence, RVOL, and the
  Bear/Bull squeeze-distance fields.
- Its 35-unit-wide HTF value cell presents the live 3-Color Smooth TEMA state:
  lime is `UPTREND`, fuchsia is `DOWNTREND`, and orange is `CONSOLIDATION`.
  This panel display is intentionally independent of the higher-timeframe EMA
  bias used elsewhere in the model. It is followed by the session state and
  London manipulation state (Bear/Bull fakeout, Asia high/low sweep, or no
  manipulation). Session direction uses the current session open when
  available; on high timeframes where no session-start bar exists, it falls
  back to the current daily open.
- HTF trend is based on the configured higher-timeframe EMA and is context only:
  `AUKŠTYN` for up, `ŽEMYN` for down, and `DIAPAZONAS` for range.
- RVOL is `barVolumeUsdt / SMA(barVolumeUsdt, whaleLength)`. It is green at
  1.5x or higher and gray below 1.0x.
- Squeeze events no longer create persistent projection lines, so the
  Bear/Bull squeeze-distance fields display `NONE`; isolated event markers and
  dashboard states are the only squeeze rendering.

3. TARGETS, INVALIDATION, AND REVERSALS
---------------------------------------

- An SSL sweep creates a Bull target at the best active BSL above price. A BSL
  sweep creates a Bear target at the best active SSL below price.
- Target selection uses zone priority first and distance second. It does not
  require BOS, CHoCH, or MSB.
- Only one yellow forecast target line can be active. A reached target becomes
  gray dashed history. A target cancelled, superseded, or expired is deleted.
- Bull targets are invalidated when price breaks below their origin sweep low.
  Bear targets are invalidated when price breaks above their origin sweep high.
  The originating dashboard row retains `REV CANCEL`.
- An invalidated Bull target immediately scans for an SSL below price and flips
  to a Bear target; an invalidated Bear target performs the inverse scan.
- Target distance controls apply before rendering. Filtered targets show
  `TGT DISTANT` in the Signal / Bias column and do not draw a line.
- Targets are constrained to the latest 20-bar Fibonacci range:
  - Bear targets cannot be below `swingHigh - swingRange * 0.618`.
  - Bull targets cannot be above `swingLow + swingRange * 0.618`.
- After the Fibonacci cap, an invalid directional Bear target is placed at
  `low - ATR(14) * 0.5`; an invalid Bull target is placed at
  `high + ATR(14) * 0.5`. This runs before the distance filter.
- On Bear BOS or Bear CHoCH, the indicator may instead target the strongest
  active Bullish Order Block below price, selected by power, touches, then
  distance. The selected block's top is used directly, without a later
  Fibonacci cap or fallback adjustment, and the live C1 dashboard cell reads
  exactly `BEAR TGT: OB`.
- A superseded origin row remains `SUP -> BEAR MOM` or `SUP -> BULL MOM`;
  later bars do not recompute it.

4. ORDER BLOCKS AND WHALE SIGNALS
---------------------------------

- Confirmed pivot lows form the active Bullish Order Block stack (green);
  confirmed pivot highs form the active Bearish Order Block stack (violet).
  Each stack retains at most five records. On a confirmed invalidation, the
  block is marked inactive for signal history and its box is deleted
  immediately, preventing historical translucent layers from accumulating.
- Bullish block invalidation requires a confirmed close below its bottom and either:
  - `deltaPercent < -25` with `relativeVolume > 1.5`; or
  - three-bar cumulative Delta below -40%.
- Bearish block invalidation is the symmetrical confirmed close above top condition with
  `deltaPercent > 25` and RVOL above 1.5, or three-bar cumulative Delta above
  40%.
- A WHALE BUY requires overlap with an active Bullish Order Block, positive
  Delta, above-average total volume, high estimated Buy volume, and no active
  bearish reversal. A non-positive-Delta test shows `OB TEST (B)`.
- A WHALE SELL applies the symmetrical Bearish Order Block, negative-Delta,
  above-average volume, and high estimated Sell-volume requirements.
- While price remains below an active Bearish Order Block above it, only the live
  C1 dashboard row can be locked to `SELL BIAS / BEAR OB`. Historical
  C2-C10 rows are never rewritten.
- A confirmed Whale Buy below an uncleared Bearish Order Block retains the
  concise `WHALE BUY` status while the active target is shown as `BEAR TGT: OB`.
- A fresh Whale Buy against an active Bear target temporarily displays
  `WHALE BUY`; following recovery bars show `WHALE BUY | ACCUMULATION` until a
  confirmed bearish setup. The inverse Whale Sell / Bull target behavior shows
  `WHALE SELL | DISTRIBUTION`. These live overrides do not delete a target line
  or rewrite history.

5. VOLUME, DELTA, CVD, AND OI
-----------------------------

- `totalVolumeUsdt` is traded volume times close. `buyVolumeUsdt` and
  `sellVolumeUsdt` are estimated directional USDT volume.
- `deltaVol` is estimated Buy volume minus Sell volume.
- `deltaPercent` is `(buyVolume - sellVolume) / (buyVolume + sellVolume) * 100`.
- Closed lower-timeframe candles are classified by candle direction. Pending
  live-bar volume is classified from incoming price movement. When intrabar data
  is unavailable, the indicator uses the parent candle's close position in its
  range. This estimate is never exchange bid/ask data.
- CVD is cumulative estimated Delta. Its slope relative to price is used for
  divergence, not its absolute value.
- At confirmed pivots, higher price high with lower CVD high is Bear CVD
  divergence; lower price low with higher CVD low is Bull CVD divergence.
  Chart triangles and the matching `BULL DIV` / `BEAR DIV` dashboard tag are
  plotted at the pivot bar.
- A separate live CVD precursor compares the current close and CVD to their
  rolling prior 3-5 bar lows/highs (four bars by default). A lower close with
  a higher CVD low reports `BULL REV (CVD)` in C1; a higher close with a lower
  CVD high reports `BEAR REV (CVD)`. It updates on every live tick and
  exclusively owns the C1 Signal cell unless a validated LSQ state is active.
  At bar close, its one-bar pulse enters the existing target lifecycle and
  bypasses normal counter-trend and failed-bull-sweep filters. The historical
  pivot-confirmed CVD triangles remain unchanged.
- OI is significant when `abs(oiDeltaUsdt)` exceeds the configured percentage of
  traded USDT volume. Positive OI with neutral Delta is only an accumulation
  candidate. It must also have Buy volume above Sell volume and a net-delta
  dominance of at least 15%, calculated as
  `abs(Buy - Sell) / max(1, Buy + Sell) * 100`; otherwise accumulation is
  disabled. Falling OI with strongly positive Delta marks a Short Squeeze;
  falling OI with strongly negative Delta marks a Long Squeeze.
- Institutional volume smoothing and KNN outcome resolution use a timeframe-adaptive
  horizon: 5 bars at 15 minutes or below, otherwise 14 bars, without exceeding
  the configured input lengths. An accumulation candidate is cleared immediately
  when price closes above its 9-period EMA and above the prior bar high, preventing
  stale institutional or Long Squeeze accumulation states during upward expansion.

6. REVERSAL QUALIFYING SETUPS
-----------------------------

Candle anatomy evaluation:
- `f_evaluateCandleAnatomy()` is evaluated once per bar and supplies the
  candle range, body, upper and lower wicks, their range-relative percentages,
  close position (0% at low, 100% at high), and green/red direction flags.
- A zero-range candle returns 0% for every anatomy percentage, preventing
  division by zero. The parent-candle fallback Buy/Sell estimate retains its
  neutral 50/50 split in that case.
- Exhaustion and Order-Block touch checks use the shared wick and close-position
  percentages. The AI2 score matrix receives the same anatomy values, including
  the Whale Distribution Wall test of a lower-half close and upper wick larger
  than the body.

Bullish sweep reversal:
- An SSL sweep occurred on the current or one of the preceding three bars.
- Current USDT volume exceeds 1.5 times the whale-volume average.
- Estimated `deltaVol` is positive.

Bearish sweep reversal:
- A BSL sweep occurred on the current or one of the preceding three bars.
- Current USDT volume exceeds 1.5 times the whale-volume average.
- Estimated `deltaVol` is negative.

Completed squeeze markers:
- A Short Squeeze Completed marker requires a BSL sweep in the last three bars,
  total USDT volume at least 1.15x its 50-bar SMA, an upper wick of at least
  35% of range, a close in the lower 55% of range, and either negative
  `deltaVol` or `deltaPercent` below 5%, and requires institutional volume,
  RVOL of at least 1.5, a recent major CVD divergence, or bullish consolidation
  flow (`chopLean == 1`). It leaves a width-4 neon-fuchsia horizontal line
  from its high extending right. Its chart text is suppressed unless it
  qualifies as an institutional purge for the unified label pipeline. The
  square and column glow are intentionally omitted. Only the most recent Short
  Squeeze line is active; a new confirmed Short Squeeze deletes the prior
  Short Squeeze line. It contributes a -30 directional score shift.
- A Long Squeeze Completed marker uses the symmetrical SSL sweep, high-volume,
  35% lower-wick, upper-55%-close, and either positive `deltaVol` or
  `deltaPercent` above -5% criteria, and requires institutional volume, RVOL
  of at least 1.5, a recent major CVD divergence, or bullish consolidation flow
  (`chopLean == 1`). It leaves a width-4 neon-cyan horizontal line from its
  low extending right. Its state is shown in the dashboard and, when paired
  with a qualifying event, through the unified label pipeline. The square and
  column glow are intentionally omitted. Only the most recent Long Squeeze
  line is active; a new confirmed Long Squeeze deletes the prior Long Squeeze
  line. It contributes a +30 directional score shift.

Bullish exhaustion reversal, available on 4H and higher:
- Price overlaps an active Bullish Order Block, or an SSL sweep occurred within
  three bars.
- The lower wick is at least 55% of the range, the candle closes green, total
  USDT volume is below 85% of its 20-bar SMA, and Delta is positive.

Bearish exhaustion reversal, available on 4H and higher:
- Price overlaps an active Bearish Order Block, or a BSL sweep occurred within
  three bars.
- The upper wick is at least 55% of the range, the candle closes red, total
  USDT volume is below 85% of its 20-bar SMA, and Delta is negative.

Order-Block touch reversal:
- A Bullish touch requires active Bullish OB overlap, a green close above the
  prior close, lower wick at least 45% of range, close in the upper 40% of range,
  and positive Delta.
- A Bearish touch requires active Bearish OB overlap, a red close below the
  prior close, upper wick at least 45% of range, close in the lower 40% of
  range, and negative Delta.
- An OB touch has precedence over exhaustion on the same candle. Its latch is
  reset only after a non-orange full exit; orange TEMA is context and does not
  suppress a qualifying setup.

Structural, probability, trap, and purge text is emitted only by the unified
four-line confirmed-bar pipeline. Separate transparent white per-candle score
annotations display the signed AI score delta, two-character matrix code, and
available KNN confidence at the TEMA level. Bullish qualifying setups require a
close in the upper 40% of range and above the prior close; Bearish setups
require a red close in the lower 40% and below the prior close.
- Orange TEMA consolidation remains available as AI-score context but does not
  suppress qualifying OB-touch or exhaustion setups, their probability state,
  or their latches.

Nearest-liquidity and Long Squeeze status tracking:
- The nearest active BSL is the closest active pivot-high level strictly above
  the close. The nearest active SSL is the closest active pivot-low level
  strictly below the close. These same levels receive the emphasized liquidity
  styling described in the Liquidity Map section.
- `f_getMarketStructureSignal()` ranks each confirmed bar as Strong, Medium,
  Weak, or Neutral Buy/Sell using BOS/CHoCH, fresh or invalidated Order Blocks,
  Order Block tests, institutional volume, Delta, candle anatomy, and the
  institutional heavy-selling crash profile.
- `f_resolveSqueezeStatus()` is a stateless per-candle classifier. It begins
  every evaluation at `NEUTRAL` and never reads a prior squeeze result,
  activation bar, invalidation level, timer, or future-candle confirmation.
- An isolated event is resolved only from the current candle's qualified
  footprint: high-volume purge, a qualifying borrowed matrix handover, a
  confirmed counter-trend CVD divergence with price rejection or signed Delta,
  or the immediate institutional volume/Delta-climax override. Positive flow
  resolves to `INSTITUTIONAL_ACCUMULATION` or `TRUE_BULL_REVERSAL`; qualifying
  negative flow resolves to `INSTITUTIONAL_PURGE`.
- Matrix handovers reuse the existing matrix code, score, reactive-flow, TEMA,
  and HTF-bias checks rather than recalculating Buy/Sell volume. Bullish
  handovers still require counter-trend buying/absorption and retain the
  uninterrupted-markdown exclusion. Bearish handovers still require a
  counter-trend selling reaction and a 60%-of-range upper-wick exhaustion
  footprint.
- A bearish event remains blocked while the bullish TEMA velocity gate sees
  consecutive higher highs, unless that same candle satisfies the peak-wick
  exhaustion check. Thus an in-trend sell tick cannot create a premature
  `LSQ PURGE`.
- No squeeze line, pending state, phase, countdown, source anchor, or
  confirmation window is retained. On the next bar, the classifier again
  returns `NEUTRAL` unless that new bar independently qualifies.
- The live `C1` dashboard row reads the current intrabar classification without
  changing confirmed state. Rows `C2`-`C10` read only their matching completed
  candle's `squeezeStatus[candleIndex]`, with no copied or shifted result.
- A 75%+ `STRONG` Bullish reversal setup stores its candle low for five bars;
  a later wick below it emits `⚠️ LONG TRAP`. A qualifying Bearish setup stores
  its high and emits `⚠️ SHORT TRAP` when a later wick breaks above it. Both
  are emitted only by the unified label pipeline.
- `f_formatDashboardSignal()` is the single source of Dashboard `SIGNAL` text.
  It composes the isolated squeeze result, structural label, market-structure
  verdict, and consolidation bias with unique ` | ` separators. Active
  `LSQ PURGE`, `LSQ ACTIVE`, and `LSQ BULL REV` events take priority over
  lower-priority context for that one candle only.

7. REVERSAL PROBABILITY AND AI CONFLUENCE
-----------------------------------------

The final Reversal Probability Score is strictly bounded from 0% through 99%.

Velocity reversal override:
- A confirmed counter-trend reversal can trigger without waiting for the
  standard sweep lookback when the prior configured number of candles (four by
  default) are consecutive directional expansion candles, each with a body of
  at least 0.5 ATR.
- The reversal candle must test a same-side active Order Block, H4 statistical
  corridor, or liquidity sweep and show an opposite signed Delta at the
  configured Bullish/Bearish Delta threshold with institutional or extreme
  volume.
- A qualifying velocity handover receives a 30-point probability weight and
  75% score floor, immediately emits the normal BULL REV/BEAR REV event, and
  uses the established target lifecycle as a bullish/bearish sweep pulse.
  Standard counter-trend and failed-bull-sweep filters do not block this pulse.

Base score:
- Sweep and exhaustion setups begin with the completed sweep-target success rate:
  `successfulSweepReversals / totalLiquiditySweeps * 100`.
  Before any tracked sweep result exists, they use a 45% baseline.
- Order-Block touch setups begin at 40%.

Standard weights:
- Volume: +15 above 2.0x the 20-bar total-volume SMA; otherwise +10 above 1.5x.
  Exhaustion setups receive no Volume weight.
- Delta: +15 when `abs(deltaVol) / volume > 0.15`; otherwise +10 when above
  0.08.
- CVD divergence: +15 when a confirmed direction-matching CVD divergence is
  within `cvdDivergenceWindow` (five bars by default).
  A Bullish qualifying setup with `BULL DIV` in that window and estimated Buy
  volume above $1,000,000 receives +25 instead. This heavy-buy confluence
  suppresses an opposing TEMA penalty.

AI confluence weights:
- SMT context: +15 for a Bullish qualifying sweep or OB-touch setup when
  `isAltStrong` is true (`ALT STRONG (BTC FALLING)`), or a Bearish qualifying
  sweep or OB-touch setup when `isAltWeak` is true (`ALT WEAK (BTC RISING)`).
  An otherwise qualifying setup facing the opposite SMT state receives -15.
- Absorption trap: +15 for a Bullish sweep reversal when `isBearBreakTrap` is
  true on this or the prior bar. The same +15 applies to a Bearish sweep
  reversal when `isBullAbs` is true on this or the prior bar.
- Dynamic H4 corridor depth: this replaces the legacy fixed +15 corridor boost
  and never applies to an OB-touch setup:
  - Bullish: `lowerBandWidth = h4Low - h4LowerCorridor`.
  - Bearish: `upperBandWidth = h4UpperCorridor - h4High`.
  - Shallow penetration: +10 when close lies in the first half of the relevant
    orange band beyond H4 Low / High.
  - Deep penetration: +20 when close reaches or exceeds the outer half of the
    relevant orange band.
- Vol Climax Abs resistance wall: +15 for a Bearish OB-touch reversal or BSL
  sweep when the setup high is within 0.05% of the most recent
  `volClimaxAbsCeiling`, provided that ceiling was established within 10 bars.
- TEMA trend confluence: +15 for a Bullish setup when the smooth chart-timeframe
  TEMA state is lime, or a Bearish setup when it is fuchsia.
- Live orange-TEMA consolidation bias feeds the directional score: a Bullish
  setup with `chopLean == 1`, or a Bearish setup with `chopLean == -1`, receives
  +10. A setup against the live lean receives -15. The final score remains
  clamped from 0% through 99%.
- Extreme institutional pressure is independent of the TEMA state: USDT volume
  above 1.8x its 20-bar SMA with Delta below -12% is heavy selling; Delta above
  +12% is heavy buying. Either contributes +30 and imposes a 75% minimum final
  score, capped at 99%. The top panel reports the matching momentum direction.
- When a reversal setup is active, AI2 compact-code confluence is folded into
  the UDF-derived score shift: `WW` or `WH` adds +30 and enforces the same 75%
  floor; `AH` adds +15; `AM` and `RM` add +5; `AL` and `RL` subtract 10.
  Other code combinations add no separate confluence.
- AI2 calculates a raw-volume `RVOL = volume / SMA(volume, whaleLength)` and
  applies the signed micro-variation `deltaPercent * 0.15 * RVOL` on every
  bar. This prevents static historical-ratio scores during consolidation while
  preserving the 0-99 clamp and extreme-pressure floor.

The confluence subtotal supplies the base change for the candle matrix. The
final aggregate advances sequentially on every bar:
`nz(aiScore[1], reversal baseline) + UDF-aligned score change + signed micro
volume Delta shift`, clamped to the 0-99 range. Thus an R1 top-rejection score
directly reduces `aiScore` rather than being bypassed by a detached weight.

When the clamped score reaches 85% or higher, `maximumAiProbability` is set
and the H4 panel uses `🔥 MAKSIMALI AI TIKIMYBĖ`. A confirmed bar at or above
that threshold also triggers the `Maximum AI reversal probability` alert.

8. HIGH-VOLUME CLASSIFICATION AND REVERSAL LABELS
-------------------------------------------------

- The dynamic High-vol threshold is recalculated every bar:
  `SMA(totalVolumeUsdt, 50) * 1.5`.
- `STRONG`: probability is at least 50%.
- `HIGH VOL`: probability is below 50% and total USDT volume meets or exceeds
  the High-vol threshold.
- `WEAK`: probability is below 50% and total USDT volume is below the threshold.
- The unified chart label has exactly four compact lines: an explicit
  structure/trap/sweep rule, probability context, estimated `B: $...`, and
  estimated `S: $...`. It never displays a `-` placeholder. Missing rule text
  resolves to `PIV L`, `PIV H`, `⚡ SWEEP`, or a qualified `VOL ACC` state.
  A qualified accumulation label includes the dominance strength: `STRONG` at
  35% or more, `MED` from 20% to under 35%, and `WEAK` from 15% to under 20%.
  An accumulation candidate below 15%, or without Buy-volume dominance, is not
  rendered. Reversal probability
  text uses `99% BEAR REV` or `99% BULL REV`; a sweep uses `99% SWEEP`.
  The H4 momentum display uses `75% BEAR MOM` or `75% BULL MOM`.
- The unified renderer joins only non-empty label components, so reversal labels
  never contain leading, trailing, or doubled newline rows. Primary event labels
  use `size.tiny` to minimize their footprint. Bullish and Bearish reversal
  labels are edge-triggered, emitting only on the first confirmed qualifying
  candle. Bearish labels use a 2.5 ATR offset above the candle high to clear
  right-axis price markers.
- A unified chart label is emitted only on a confirmed bar with structural or
  probability text, an Order-Block touch reversal, or an SSL/BSL sweep.
  Any emitted text containing `Bear` or `BEAR`, plus explicit Bearish
  continuation or reversal state, is strictly orange and prints above price.
  All other non-purge labels are teal and print below price. Institutional
  purge labels retain their distinct magenta background and use their squeeze
  side for placement.
- A confirmed matrix-driven `INSTITUTIONAL_ACCUMULATION` event prints a small
  mint `LSQ` up triangle below that same candle. A confirmed
  `INSTITUTIONAL_PURGE` event prints a small fuchsia `PURGE` down triangle
  above that same candle. These markers read the same isolated status series as
  the C1-C10 dashboard and have no source/monitoring phase, delayed removal, or
  forward state.
- The newest confirmed `SHORT TRAP` or `BEAR REV` label is tracked as the active
  bearish signal. A later confirmed close below that signal candle's low grays
  the label and clears its active state. A new confirmed Bearish reversal/trap
  supersedes the prior active bearish signal by graying the obsolete label;
  historical labels remain on the chart.
- A generic `SWEEP` must agree with the dominant estimated directional volume:
  bearish labels require Sell >= Buy and bullish labels require Buy >= Sell.
  `VOL ACC` additionally requires the strict 15% net-delta dominance gate, so
  balanced high-volume churn cannot create an accumulation label.
- Orange TEMA consolidation does not suppress reversal-probability processing
  or qualifying setups; heavy buying and selling pressure is explicitly
  eligible for the matching directional score. Order-Block-touch latches remain
  paused during orange consolidation so a suppressed touch does not consume its
  future opportunity. A fuchsia TEMA reduces the Bearish sweep volume gate from
  1.5x to 1.125x (a 25% reduction).
- A bullish unified-label candidate requires both lime smooth TEMA and
  estimated Buy >= Sell volume. Sell-dominant bullish candidates clear their
  pending label text and are not rendered.
- The only candle-score override is a flow-confirmed institutional event:
  `WW`, or `AH` / `AM` with a positive score change of at least +7 and
  aggregate estimated volume of at least $500M. The override itself still
  requires Buy >= Sell volume.
  Bearish continuation, Bearish reversal, Bearish Order-Block touch, and BSL
  sweep labels remain eligible during a downtrend.
- Historical score and matrix values remain available to internal calculations,
  the dashboard, and the Data Window without local chart text.
- The H4 probability row displays only the rounded percentage and compact
  directional code (`BEAR MOM`, `BULL MOM`, `BEAR REV`, or `BULL REV`).
  Existing direction-relative colors are retained.

9. ABSORPTION, SMT, AND MID-RANGE CONTEXT
-----------------------------------------

VOL CLIMAX ABS:
- An institutional volume spike occurs at an SSL or BSL sweep with positive
  Delta.
- The prior candle is green and current `deltaPercent` is below the prior
  candle's `deltaPercent`.
- Price is at the H4 upper-corridor confluence.
- The yellow `VOL CLIMAX ABS` circle is plotted, an orange ceiling line is
  created, and the latest ceiling price and origin bar are retained for the
  resistance-wall score.

Absorption-trap markers:
- A Bear BOS or Bear CHoCH with estimated Buy volume more than 1.5x estimated
  Sell volume is a Bull Absorption Anomaly, marked by a green
  `Bear Break Trap` square below the bar.
- A Bullish internal CHoCH with estimated Sell volume more than 1.5x estimated
  Buy volume is a Bear Absorption Anomaly, marked by a red
  `Bull Break Trap` square above the bar.

BTC SMT:
- Bearish SMT / ALT WEAK requires BTC rising over the configured lookback while
  the chart asset is failing. It appends `ALT WEAK (BTC RISING)`.
- Bullish SMT / ALT STRONG requires BTC falling while the chart asset holds. It
  appends `ALT STRONG (BTC FALLING)`.
- SMT is suppressed on BTC charts and when the chart symbol is the configured
  BTC reference asset, preventing self-comparison. SMT is intentionally
  dashboard-and-score only; it does not draw candle arrows or overlays.

Mid-range:
- Mid-range volume requires USDT volume above 1.25x its 20-bar SMA and a candle
  strictly inside the locked H4 range and statistical corridors.
- Delta above +20% adds `MID BULL`; Delta below -20% adds `MID BEAR`.
- Three-bar hidden price/CVD divergence adds `CVD ABSORP`.
- Mid-range and SMT messages append to existing Signal / Bias text with `|`;
  they do not replace a primary sweep, target, Whale, or Order Block message.

10. DASHBOARD AND GRAPHIC LEGEND
--------------------------------

Tables:
- The bottom-right dashboard uses 10 recent-candle rows (`C1-C10`) on every
  timeframe and these columns: `Bar`, `BUY`, `SELL`, `TOTAL`, `DELTA`, `CMF`,
  `OI Δ`, and `SIGNAL`.
- The top-right H4 panel is a two-column, six-row table. Its header is
  `H4 MAP` / `Live`; its five data rows are `H4 Δ`, `REV %`, `HTF`,
  `CVD/RVOL`, and `SQ Δ`.
- The optional bottom-left failure table shows the five most recent invalidated
  Order Blocks with `Time`, `Type`, `Close Price`, and `Volume`.
- Dashboard headers and data rows use `size.tiny` text with centered horizontal
  and vertical alignment. The bottom-right grid uses explicit readable widths:
  `Bar` 5%, `BUY`/`SELL` 8%, `TOTAL`/`CMF` 8%, `DELTA` 8%, `OI Δ` 10%, and
  `SIGNAL` 37%. No zero-width or forced auto-compression is applied.
- Dashboard `BUY`, `SELL`, and `TOTAL` USDT figures use one decimal with compact
  K/M/B notation (for example `$545.4M`). The OI column and chart-label volumes
  retain whole-unit compact notation; the underlying values and thresholds are
  unaffected.
- Dashboard percentages and ratio-style metrics use exactly one decimal place:
  Delta, reversal probability, H4 distance, squeeze distance, CMF, RVOL, and
  embedded Signal-column percentage values use the `#.0` format.
- CMF is an internal ratio and is displayed in the dashboard as a percentage:
  `str.format("{0,number,#.0}%", cmfValue * 100)` renders exactly one decimal
  place (for example, internal `-0.13` renders as `-13.0%`).

- Target-hit rows are blue. Cancelled-target origins are orange. Superseded target
  origins are dark charcoal.
- `SIGNAL` owns sweep, target, target-hit, target-distance, failure,
  reversal, Whale, CVD-divergence, SMT, and context messages.
- Consolidation context is compact: `CONSOL (ACC)`, `CONSOL (DIST)`, or
  `CONSOL (NEUT)`. Market verdicts are rendered as `STRONG S`/`MED S`/
  `WEAK BIAS` or `STRONG B`/`MED B`/`WEAK B`. The combined exceptions are
  `ROB / WEAK BIAS`, `IT (S) / NEUT`, and `REV CANCEL (S)`.
- Completed squeeze signals use compact dashboard codes: `SSQ` for a Short
  Squeeze and `LSQ` for a Long Squeeze. `SSQ` uses a dark-slate-blue
  background with vibrant pink text; `LSQ` uses a deep-navy background with
  bright cyan text. This squeeze styling takes precedence over other Signal /
  `SIGNAL` background states.
- The dashboard is rendered only for the live last bar or last confirmed history.

Lines, targets, and zones:
- Active BSL is mint/teal and active SSL is crimson/pink. Standard levels are
  thin and dotted; high-volume levels are wider and more opaque; EQH/EQL levels
  are solid, width 3, and fully opaque. Mitigated levels and reached targets
  become gray dashed history. A target that is cancelled, superseded, distant,
  or expired is removed.
- Locked H4 High and Low are width-2 steplines. The orange H4 statistical
  corridors are width-2 steplines with a transparent orange fill back to their
  associated locked level. On lower timeframes, optional mint/pink dashed H4
  projections extend right.
- The sole active forecast is a width-2 yellow line extending right. Target
  names remain dashboard context so no local target tag overlaps chart labels.
- A `VOL CLIMAX ABS` event creates a width-2 orange absorption ceiling at the
  candle high. It extends right until price closes above it, then becomes gray
  dashed history and stops extending.

Order Blocks and structural labels:
- Active Bullish Order Blocks are transparent green rectangles; active Bearish
  Order Blocks are transparent violet rectangles. Both begin at their confirmed
  pivot and extend right. Invalidated blocks turn gray and stop extending.
- Bullish and Bearish Order Blocks have no local chart text. Their power,
  directional volume, and state remain available to the dashboard and target
  logic without overlapping the unified event label.
- An exhaustion reversal can create a transparent orange supply rectangle from
  the exhaustion candle's high to low. It extends right only during its short
  tracking window, then stops on invalidation or completion. Its dashboard
  state is `AWAITING MOMENTUM`, `EXH CONFIRMED`, or `DISTRIB. ZONE`.
- Bear BOS, Bear CHoCH, and LTF CHoCH events contribute their short trigger
  name to the unified confirmed-bar chart label. A combined bearish event
  appends `+ LTF Bear CHoCH`. The label uses the ATR-derived collision offset
  and is raised further when needed to avoid a nearby reversal label.
- A Bull Absorption Anomaly has a green `Bear Break Trap` square below the
  break; a Bear Absorption Anomaly has a red `Bull Break Trap` square above
  the break.

Candle markers and annotations:
- Every confirmed candle emits its AI score-delta and matrix-code annotation at
  the synchronized TEMA coordinate. Consolidation, chop, and reversal-label
  conditions do not suppress these historical annotations; the unconfirmed
  live candle uses one replaceable annotation refreshed on every tick.
- A tiny mint up triangle below a confirmed pivot marks Bull CVD divergence;
  a tiny crimson down triangle above a confirmed pivot marks Bear CVD
  divergence. Their directional volume remains dashboard/data-window context
  rather than local chart text.
- A yellow circle above a candle marks `VOL CLIMAX ABS`. A semi-transparent
  green circle below or red circle above marks `MID BULL` or `MID BEAR`.
  Semi-transparent orange down/up triangles mark Bull/Bear hidden CVD
  absorption, respectively. An orange down triangle above a candle marked
  `EXH` is an exhaustion reversal.
- An aqua diamond below a candle marks institutional accumulation. A fuchsia
  up triangle below marked `SQ` is an aggressive Short Squeeze; an orange down
  triangle above marked `LQ` is an aggressive Long Squeeze.
- OI accumulation and squeeze reductions retain their markers without local
  numeric chart annotations.
- Completed Short and Long Squeezes retain their fuchsia-above and cyan-below
  horizontal projection lines. Their squares, column fills, and local status
  labels are intentionally not rendered.
- An invalidated Bullish or Bearish Order Block has a gray X below or above its
  candle. Failure details remain available through Pine Logs and the optional
  failure table without a local diagnostic label.
- BTC SMT remains dashboard-and-score only in AI2. It deliberately does not
  render chart arrows or overlay markers.
- The 3-Color CM Institutional TEMA is lime for a rising trend, fuchsia for a
  falling trend, and orange during ATR-defined consolidation.

Data Window only:
- When `Expose per-candle metrics in Data Window (CSV export)` is enabled, the
  following values are available for inspection or CSV export but are not chart
  plots: Reversal Probability %, Delta %, CMF, CVD, RVOL, Buy Volume (USDT),
  Sell Volume (USDT), Sweep Penetration %, Reversal Flag, CVD Divergence Flag,
  and HTF Trend.

11. INSTITUTIONAL TEMA BIAS LINE
--------------------------------

- `CM TEMA Length` defaults to 20, `CM Color Smoothing Shift` defaults to 2,
  and `Consolidation ATR Sensitivity Multiplier` defaults to 0.15.
- The line uses the CM-style TEMA formula
  `3 * (EMA1 - EMA2) + EMA3` directly on the chart timeframe, so it remains
  smooth and aligned with the price candles.
- Orange is the default state and remains active whenever the absolute TEMA
  slope over the smoothing shift is below ATR(14) multiplied by the
  consolidation multiplier. Outside consolidation, it is lime when price is
  above the rising smooth TEMA and fuchsia when price is below the falling
  smooth TEMA.
- During orange consolidation, an internal lean uses responsive USDT volume and
  Delta thresholds: volume above 1.10x its 20-bar USDT SMA plus Delta above
  +12% is `ACCUMULATING BULL`; Delta below -12% is `DISTRIBUTING BEAR`;
  otherwise it is `PURE NEUTRAL`. The dashboard appends the applicable
  `BIAS: 🟡 CONSOLIDATION (...)` text in its Signal / Bias cell.
- In `Last Candle Volume Info_AI2.pine`, the unified
  `f_getSynchronizedScoreChange(isWhaleBuy, isWhaleSell, baseScore, vMult)`
  function calculates every historical and live score shift. `baseScore` is
  the change in the aggregate score before its AI2 micro-volume adjustment;
  `vMult` is RVOL times the configurable `volSensitivity` input.
- AI2 RVOL is `volume / SMA(volume, whaleLength)`, where `whaleLength`
  defaults to 20 and `volSensitivity` defaults to 1.0. Low-volume structural
  scores are correspondingly reduced, while Whale overrides use a minimum
  multiplier of 1.0.
- The score matrix uses a compact two-character code: first character
  `W` for Whale override or distribution wall, `A` for absorption/rejection,
  or `R` for regular continuation/mute; second character is RVOL tier:
  `W` (>= 2.0), `H` (>= 1.5), `M` (>= 0.85), or `L` (< 0.85).
- Whale Buy and Whale Sell signals have absolute precedence after the
  distribution-wall check: both use the `W` family, with scores of
  `+15 * max(1.0, vMult)` and `-15 * max(1.0, vMult)`, respectively.
- The Whale Distribution Wall is also in the `W` family. It requires an H4
  upper-corridor touch or active Bearish OB overlap, Delta above +12%, a close
  in the lower half of the range, and an upper wick larger than the body. It
  produces `-20 * RVOL` before other Whale handling.
- Without a Whale signal, a Green candle with an upper wick greater than 1.2x
  its body and 40% of its range is in the `A` family, with `-10 * vMult`.
  Other Green candles are in the `R` family: a negative base score is muted
  to zero, otherwise the
  positive base score (or +5 baseline) is scaled by `vMult`.
- Without a Whale signal, a Red candle with a lower wick greater than 1.5x its
  body is in the `A` family, with `+10 * vMult`. Other Red candles are in the
  `R` family: positive base
  scores are inverted and scaled; zero uses `-5 * vMult`; existing negative
  scores are scaled negative.
- UDF score deltas, matrix codes, and KNN confidence are stamped as transparent
  white text on each confirmed non-probability candle and on the live unconfirmed
  candle. The historical and live annotations use the same synchronized
  `deltaTextStr` value. Reversal classifications remain in the unified event
  label rather than creating a separate probability label.

12. KNN CONFIDENCE SCORE (AI INDICATOR ONLY)
--------------------------------------------

- The KNN classifier evaluates new Bullish and Bearish sweep-reversal events,
  not exhaustion-only or OB-touch-only setups.
- Features are clamped Delta percent, current RVOL, and normalized close position
  inside the H4 corridor.
- A confirmed event stores its feature set, direction, entry close, and a pending
  outcome. After the configurable outcome horizon (five bars by default), a Bull
  sample succeeds above its entry close and a Bear sample succeeds below it.
- The memory retains at most 200 complete samples. Prediction uses Euclidean
  distance over the five nearest resolved samples.
- `aiConfidenceScore` is available only once five resolved feature-complete
  samples exist. It remains calculation and dashboard context; qualifying
  reversal probability is shown through the unified event-label pipeline.
- KNN confidence is informational: it does not alter Reversal Probability,
  classification or targets.
- When close is within 0.30% of an active squeeze line and raw volume is at
  least 1.25x its 20-bar SMA, the same resolved-sample KNN lookup runs as a
  proactive pre-sweep query. A confidence of at least 60% enables its matching
  alert without creating a local chart label; no synthetic fallback confidence
  is used.

13. RELEVANT DEFAULTS
---------------------

- `volumeLength`: 20; `highVolumeMultiplier`: 1.0.
- `institutionalVolumeSmaLength`: 20; `institutionalVolumeMultiplier`: 1.8.
- `whaleLength`: 20; `whaleMultiplier`: 2.0.
- `negativeDeltaThreshold`: -12%; `momentumDeltaThreshold`: 15%;
  `momentumCmfThreshold`: 0.05.
- `oiSignificancePercent`: 15%; `neutralDeltaBound`: 15%;
  `bullishDeltaThreshold`: 15%; `bearishDeltaThreshold`: -12%.

`absorptionSmaLength` and `absorptionVolumeMultiplier` remain exposed settings
but do not currently feed a separate signal, filter, or trigger.

14. AI2 RETROSPECTIVE PATTERNS AND SCORE DISPLAY
------------------------------------------------

- `AI2` evaluates Bull Breakout and Bear Trap patterns retrospectively: bar
  `[1]` supplies the setup and the current bar validates follow-through.
- Confirmed Bull Breakouts render a teal up triangle; confirmed Bear Traps
  render a fuchsia down triangle. The live confirmation preview is compact:
  `[AI 🟢]` or `[AI 🔴]`.
- The AI2 score display passes active Whale Buy/Sell conditions to the unified
  score-change function. Retrospective pattern triggers still render their
  teal/fuchsia icons but do not override the RVOL score matrix.
- Each confirmed non-probability candle also shows its signed score change and
  matrix classification directly on the chart, such as `+3`, `-2`, `RL`, or
  `AL`; KNN confidence appears below those codes when available. The current
  unconfirmed candle uses a single replaceable live annotation.
